# AGENTS.md

This file provides guidance to Qoder (qoder.com) when working with code in this repository.

## Repository Overview
This is a minimal repository containing simple text files. The project consists of:
- `hello.txt` - Contains greeting text
- `world.txt` - Contains world text
- Basic git version control with 3 commits showing iterative corrections

## Development Workflow
Since this is a simple text-based repository, standard development practices apply:
- Make changes to files as needed
- Commit changes with descriptive messages
- Push to remote repository when ready

## Common Operations
```bash
# Check repository status
git status

# View commit history
git log --oneline

# Add and commit changes
git add .
git commit -m "Descriptive commit message"

# Push changes
git push origin main
```

## Architecture Notes
This repository follows a flat file structure with no complex architecture. Files are organized at the root level with simple text content.